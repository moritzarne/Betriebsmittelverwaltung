@model IEnumerable<Betriebsmittelverwaltung.Models.Retourenverwaltung>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Typ)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kaufdatum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Wartungsintverall)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Verfügbar)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Typ)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kaufdatum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Wartungsintverall)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Verfügbar)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
